{
  "_group": "config",
  "_name": "PROJECT",
  "uri": "config/PROJECT",
  "methods": "GET",
  "schema": {
    "PROJECT": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "type": "object",
      "description": "Project Information (PROJECT). Retrieves the opened project's file dates and design code preferences; the request carries no meaningful body (DTO_EMPTY), so the fields are driven entirely by the currently opened document.",
      "properties": {
        "Argument": {
          "type": "object",
          "description": "Empty request body (DTO_EMPTY); no input fields are consumed by the handler."
        }
      }
    }
  },
  "example": {
    "Argument": {}
  }
}
