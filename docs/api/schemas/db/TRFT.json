{
  "_group": "db",
  "_name": "TRFT",
  "uri": "db/TRFT",
  "methods": "POST, GET, PUT, DELETE",
  "schema": {
    "TRFT": {
      "type": "object",
      "description": "Torsional Reduction Factor (TRFT). Keyed by element id; the factor is applied per element and can only be assigned to BEAM-type elements.",
      "properties": {
        "FACTOR": {
          "type": "number",
          "default": 1.0,
          "description": "Torsion reduction factor applied to the element. Optional; defaults to 1.0. The target element must be a BEAM-type member element or the assignment is rejected."
        }
      },
      "additionalProperties": false
    }
  },
  "example": {
    "Assign": {
      "1": {
        "FACTOR": 0.8
      }
    }
  }
}
