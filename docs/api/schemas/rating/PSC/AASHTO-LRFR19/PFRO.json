{
  "_group": "rating",
  "_name": "PFRO",
  "uri": "rating/PSC/AASHTO-LRFR19/PFRO",
  "methods": "POST, GET, PUT, DELETE",
  "schema": {
    "PFRO": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "type": "object",
      "description": "Print Check Position for Rating Output (PFRO). The single field PART selects which member end(s) the rating output/print check applies to.",
      "properties": {
        "PART": {
          "type": "integer",
          "enum": [0, 1, 2],
          "description": "Print check position. 0 = I-end, 1 = J-end, 2 = Both ends. Handler validates the value is in the range 0-2 and throws otherwise.",
          "minimum": 0,
          "maximum": 2
        }
      },
      "required": [
        "PART"
      ]
    }
  },
  "example": {
    "Assign": {
      "1": {
        "PART": 2
      }
    }
  }
}
